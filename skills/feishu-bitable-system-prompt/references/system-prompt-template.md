# 飞书多维表格 AI 系统提示词模板

把下面模板按实际业务替换后，可直接丢给飞书多维表格 AI。

```text
请为我搭建一个飞书多维表格业务系统。

系统目标：
- 这个系统主要用来解决：
- 本次先覆盖：
- 暂时不做：

使用角色：
- 角色1：负责
- 角色2：负责
- 角色3：负责

请从每个角色的实际使用场景出发设计系统，不要只按管理员视角设计。

核心数据对象：
1. 数据表A
字段：
- 字段名 - 字段类型 - 说明

2. 数据表B
字段：
- 字段名 - 字段类型 - 说明

对象关系：
- 一个A可以对应多个B
- 一个B只能属于一个A
- 如果存在多对多关系，请自动设计中间表

关键业务规则：
- 
- 
- 

需要的视图：
- 给执行角色的视图
- 给管理角色的视图
- 给个人使用的视图

需要的仪表盘：
- 要看哪些核心指标
- 按什么维度统计
- 希望有哪些图表

需要的工作流：
- 当什么事件发生时，执行什么动作
- 通知谁
- 哪些情况需要提醒、审批、升级或告警

权限要求：
- 谁看全部
- 谁只看本人
- 谁只看本组
- 哪些字段敏感

示例数据要求：
- 请生成符合业务逻辑的示例数据
- 日期、金额、状态流转、命名要合理

验收标准：
- 字段类型正确
- 表关系正确
- 关键公式可计算
- 视图可直接使用
- 仪表盘可直接看核心指标
- 工作流和权限设计完整

如果信息已经足够，请直接进入专家模式生成系统设计文档并开始搭建。
如果仍有关键缺口，请先只追问我最多 3 个最关键的问题。
```

## 使用提醒

- 模板只是骨架，真正影响效果的是具体规则
- 字段类型尽量写清，不要全写“文本”
- 多对多关系、统计口径、权限边界最容易漏
- 复杂场景优先用专家模式，不要强行默认模式
